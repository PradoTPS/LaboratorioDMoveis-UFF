<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="labmoveis.uffeventos">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_uff_eventos"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_uff_eventos"
        android:supportsRtl="true"
        android:theme="@style/semActionBar">
        <activity android:name=".Events" />
        <activity android:name=".Cadastro" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CadastraEvento"></activity>
        <activity android:name=".InformacaoEvento" />
    </application>

</manifest>